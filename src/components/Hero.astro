<section id="inicio" class="relative pt-36 pb-28 bg-white overflow-hidden">

  <!-- Glow Background -->
  <div class="absolute top-0 right-0 w-[500px] h-[500px] bg-primary-100 rounded-full blur-[140px] opacity-40"></div>

  <div class="max-w-7xl mx-auto px-6 grid md:grid-cols-2 gap-20 items-center relative z-10">

    <!-- Texto -->
    <div class="space-y-8">

      <!-- Tag superior -->
      <div class="inline-flex items-center gap-2 bg-primary-50 text-primary-600 px-4 py-2 rounded-full text-sm font-medium">
        <span class="w-2 h-2 bg-primary-500 rounded-full animate-pulse"></span>
        Educación con excelencia
      </div>

      <!-- Título -->
      <h1 class="text-4xl md:text-6xl font-bold leading-[1.1] tracking-tight text-neutral-900">
        Unidos para educar, <br />
        <span class="text-primary-600">
          formando líderes del mañana
        </span>
      </h1>

      <!-- Descripción -->
      <p class="text-lg text-neutral-600 max-w-xl leading-relaxed">
        En Academia The Number One brindamos formación académica sólida,
        acompañamiento personalizado y una metodología enfocada en resultados reales,
        preparando estudiantes para los retos del futuro.
      </p>

      <!-- Botones -->
      <div class="flex flex-wrap gap-4 pt-6">

        <a
          href="#programas"
          class="inline-flex items-center gap-2 bg-primary-600 text-white px-7 py-3 rounded-full font-medium hover:bg-primary-700 transition-all duration-300 shadow-md hover:shadow-lg"
        >
          Ver Programas
        </a>

        <a
          href="#contacto"
          class="inline-flex items-center gap-2 border border-primary-200 text-primary-600 px-7 py-3 rounded-full font-medium hover:bg-primary-50 transition-all duration-300"
        >
          Más Información
        </a>

      </div>

    </div>

    <!-- Imagen -->
    <div class="relative">

      <!-- Glow sutil detrás de imagen -->
      <div class="absolute inset-0 bg-primary-50 rounded-3xl blur-2xl opacity-50 scale-110"></div>

      <img
        src="/hero.jpeg"
        alt="Estudiantes aprendiendo en aula"
        class="relative rounded-3xl shadow-2xl w-full object-cover"
      />

    </div>

  </div>

</section>